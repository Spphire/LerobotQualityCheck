interface:
  display_name: "Manage LeRobot QC Datasets"
  short_description: "Load and switch multiple QC datasets without clearing labels."
  default_prompt: "Use $switch-lerobot-qc-dataset to load one or more local or SSH-backed datasets on the requested QC service."
